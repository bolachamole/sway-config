Os diretórios devem ser movidos para ~/.config/

Rodar ```git clone https://github.com/neovim/nvim-lspconfig ~/.config/nvim/pack/nvim/start/nvim-lspconfig```

Para LSP de C/C++: ```dnf install clangd```

Para LSP de Python: ```npm install -g pyright```
